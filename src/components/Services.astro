---
import { services } from '../data/site';
---

<section id="services" class="py-24 px-6 max-w-7xl mx-auto">
	<div class="text-center mb-16">
		<h2 class="text-4xl font-bold text-brand-dark mb-4 font-serif">Garden Design Services</h2>
		<p class="text-gray-600 max-w-2xl mx-auto leading-relaxed">
			Premium garden design in Ireland — thoughtful, buildable, and designed with long-term upkeep in
			mind from the very first sketch.
		</p>
		<div class="w-24 h-1 bg-brand mx-auto opacity-50 mt-6"></div>
	</div>

	<div class="grid md:grid-cols-3 gap-12">
		{
			services.map((service) => (
				<div class="bg-white p-8 border border-brand-border shadow-sm hover:shadow-md transition-shadow">
					<h3 class="text-xl font-bold text-brand-dark mb-3 font-serif">{service.title}</h3>
					<p class="text-gray-600 leading-relaxed">{service.description}</p>
				</div>
			))
		}
	</div>
</section>
